# Local Flash plug-in directory

Place the locally supplied 32-bit PPAPI Flash binary here as:

`Flash/pepflashplayer.dll`

For the initial compatibility test, the expected plug-in version is `29.0.0.140`, matching the existing working legacy browser package.

The DLL is intentionally excluded from Git and public releases.
